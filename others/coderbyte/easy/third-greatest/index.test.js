const ThirdGreatest = require("./index");

describe("take the array of strings stored in strArr and return the third largest word within it.", () => {
  it("should fail auto generated test", () => {
    expect(ThirdGreatest(["coder", "byte", "code"])).toBe("code");
  });
});