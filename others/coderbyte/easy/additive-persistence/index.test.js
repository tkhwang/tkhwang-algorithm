const AdditivePersistence = require("./index");

describe("AdditivePersistence", () => {
  it("should return its additive persistence which is the number of times you must add the digits in num until you reach a single digit.", () => {
    expect(AdditivePersistence(19)).toBe(2);
  });
});

describe("AdditivePersistence", () => {
  it("should return its additive persistence which is the number of times you must add the digits in num until you reach a single digit.", () => {
    expect(AdditivePersistence(19)).toBe(2);
  });
});

describe("AdditivePersistence", () => {
  it("should return its additive persistence which is the number of times you must add the digits in num until you reach a single digit.", () => {
    expect(AdditivePersistence(19)).toBe(2);
  });
});