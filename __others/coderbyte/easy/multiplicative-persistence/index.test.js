const MultiplicativePersistence = require("./index");

describe("MultiplicativePersistence()", () => {
  it("should return its multiplicative persistence which is the number of times you must multiply the digits in num until you reach a single digit.", () => {
    expect(MultiplicativePersistence(25)).toBe(2);
  });
});

describe("MultiplicativePersistence()", () => {
  it("should return its multiplicative persistence which is the number of times you must multiply the digits in num until you reach a single digit.", () => {
    expect(MultiplicativePersistence(10)).toBe(1);
  });
});

describe("MultiplicativePersistence()", () => {
  it("should return its multiplicative persistence which is the number of times you must multiply the digits in num until you reach a single digit.", () => {
    expect(MultiplicativePersistence(25)).toBe(2);
  });
});

describe("MultiplicativePersistence()", () => {
  it("should return its multiplicative persistence which is the number of times you must multiply the digits in num until you reach a single digit.", () => {
    expect(MultiplicativePersistence(39)).toBe(3);
  });
});